<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Фоновая форма кнопки -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/dark_gray" /> <!-- Цвет кнопки -->
            <corners android:radius="5dp" /> <!-- Закругление углов, если нужно -->
        </shape>
    </item>
    <!-- Обводка кнопки -->
    <item android:right="1dp" android:left="1dp" android:top="1dp" android:bottom="1dp">
        <shape android:shape="rectangle">
            <stroke android:color="@color/dark_gray"
                android:width="2dp" /> <!-- Толщина обводки -->
            <corners android:radius="5dp" /> <!-- Закругление углов обводки -->
        </shape>
    </item>
</layer-list>
